const app = () => {
  // prompt questions and answers (city name required, forecast default to No)
  // get current weather data by city name
  // if error handle error
  // if forecast YES
  // get lat and lon from current weather data
  // get forecast data by lat and lon
  // if error handle error
  // else forecast NO
  // forecast is empty
  // transform current weather
  // transform forecast weather
  // display result with current and forecast
};

module.exports = { app };
