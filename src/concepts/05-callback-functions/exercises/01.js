/*
    1.  Declare a function reduce which accepts the following parameters:
        - array: an array of numbers
        - operator: a string enum of "ADD", "SUBTRACT" or "MULTIPLY"
        - accumulator: initial value of the accumulator
    2.  The reduce function should iterate over each item in the array and apply the corresponding operation
    3.  The reduce function should return the final accumulated result
    4.  The reduce function should return an error message for an invalid operator "Invalid operator!"
*/
