/*
    1.  Declare a function getDataFromURL which accepts a url as an argument
    2.  getDataFromURL should use axios to make a request to the given url 
    3.  Log the following information in an object with the following keys:
        - username: <login key in response>
        - imageUrl: <avatar_url key in response>
        - profileUrl: <html_url key in response>
        - name: <name key in response>
    4.  If there is an error log the following message "Failed to retrieve data"

    Hint: make sure your function returns the resulting Promise
*/
